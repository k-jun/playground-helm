apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "playground-helm.fullname" . }}-test-connection"
  labels:
    {{- include "playground-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "playground-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
